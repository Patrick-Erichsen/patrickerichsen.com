<section class="subscribe-form" aria-labelledby="subscribe-heading">
  <div>
    <h2 id="subscribe-heading">Subscribe</h2>
    <p>Get new posts by email.</p>
  </div>
  <iframe
    src="https://patrickerichsen.substack.com/embed"
    width="480"
    height="320"
    frameborder="0"
    scrolling="no"
    title="Subscribe to Patrick Erichsen on Substack"></iframe>
</section>

<style>
  .subscribe-form {
    margin-top: 4rem;
    padding-top: 2rem;
    border-top: 1px solid color-mix(in srgb, var(--color-fg) 10%, transparent);
  }

  .subscribe-form h2 {
    margin: 0;
    font-size: 1rem;
    font-weight: 400;
    color: var(--color-fg);
  }

  .subscribe-form p {
    margin: 0.25rem 0 1rem;
    color: var(--color-fg-muted);
  }

  .subscribe-form iframe {
    display: block;
    width: min(100%, 30rem);
    max-width: 100%;
    border: 1px solid color-mix(in srgb, var(--color-fg) 12%, transparent);
    background: white;
  }

  @media (max-width: 520px) {
    .subscribe-form iframe {
      height: 300px;
    }
  }
</style>
